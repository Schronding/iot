/* In the .h I don't need to specify the names of the variables. I can just put the variable types*/
int linear_search(float [], float, int); 
float * BubbleSort(float [], int);
void PrintArrayFlo(float [], int );
void PrintArrayInt(int [], int ); 
float MinMax(float arr[], int max_array, char * type); 

